__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
env.bak/
venv.bak/
.env

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Git
.git/
.gitignore

# SQLite databases (if you don't want to copy your local development DB into the image)
# We handle the DB through volume mounts, not by copying it into the image layer
civic_issues.db
